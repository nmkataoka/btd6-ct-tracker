export const settings = {
  /** How long claims exist before they're automatically removed, in minutes */
  claimDuration: 30,

  /** If someone attempts to claim an owned tile, they will receive a warning if there is more than X minutes estimated owned duration left. */
  remainingOwnedWarning: 120,
}
